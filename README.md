# Labseq

## Backend (Quarkus, Java 21)

### Build
```bash
cd backend
mvn clean package
```

### Run (dev)
```bash
cd backend
mvn quarkus:dev
```
### Run with Docker
From repo root:
```bash
docker compose up --build
```
